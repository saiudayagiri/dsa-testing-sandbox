# Problem: Two Sum

Given an array of integers `nums` and an integer `target`, return indices of the two numbers such that they add up to `target`.

## Constraints
* `2 <= nums.length <= 10^4`
* `-10^9 <= nums[i] <= 10^9`
* `-10^9 <= target <= 10^9`
* Only **one valid answer** exists.

## Target Assignment Instructions
Open `test_two_sum.py`. Your job is to construct robust unit test variants using `pytest`.
Your test portfolio must cover standard arrays, negative values, minimum constraint bounds, and maximum range limits to pass.
